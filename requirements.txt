# GUI 框架
PySide6>=6.5

# LLM API（OpenAI 兼容协议，覆盖 DeepSeek/OpenAI/通义/Kimi/智谱/Gemini/OpenRouter）
openai>=1.30

# 电子书解析（EPUB 文本层 / PDF 文本层提取）
PyMuPDF>=1.24

# 开发/打包（可选）
# pyinstaller>=6.0   # 打 exe 用：python -m PyInstaller --noconsole --name BookRPG main.py
